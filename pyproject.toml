[project]
name = "homelab"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13.0"
dependencies = [
    "ansible>=11.1.0",
    "sshconf>=0.2.7",
]

[dependency-groups]
dev = [
    "molecule>=25.3.1",
    "molecule-docker>=2.1.0",
]
